# Android Frontend (Compose + Apollo Kotlin + Hilt + Orbit)

## Commands

```bash
cd android
./gradlew :app:generateApolloSources
./gradlew test
```

## Architecture

- Orbit MVI state/intent flow
- Hilt modules for dependency injection
- Apollo Kotlin generated GraphQL models
