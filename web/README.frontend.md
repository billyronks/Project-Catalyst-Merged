# Web Frontend (Refine + AntD + React Query)

## Stack

- `@refinedev/core`
- `@refinedev/antd`
- `@refinedev/react-query`
- `antd` 5.x

## Commands

```bash
npm install
npm run dev
npm run codegen
npm run test
```

## Environment

Use `.env.local` values from root `.env.example`.
