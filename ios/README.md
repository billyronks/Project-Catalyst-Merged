# iOS Frontend (SwiftUI + Apollo iOS + TCA)

## Commands

```bash
cd ios
swift package resolve
swift test
```

## Architecture

- TCA reducer/state/action setup for feature flows
- Apollo operations stored in `Sources/GraphQL`
